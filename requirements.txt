pytenable>=1.5.0
python-dateutil>=2.8.2
tabulate>=0.9.0
requests>=2.31.0
mcp[cli]>=1.0.0

# Optional LLM providers (install as needed)
# openai>=1.0.0          # For OpenAI (GPT-4, etc)
# anthropic>=0.18.0      # For Anthropic API
